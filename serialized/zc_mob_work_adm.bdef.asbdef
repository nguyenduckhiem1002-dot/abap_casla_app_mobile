
    projection;
    strict ( 2 );
    use draft;

    define behavior for ZC_MOB_Work_Adm alias MobileWork
    use etag
    {
      use create;
      use update;
      use action Edit;
      use action Activate;
      use action Discard;
      use action Resume;
      use action Prepare;
    }
