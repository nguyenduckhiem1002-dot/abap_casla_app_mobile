projection;
strict ( 2 );
use draft;

define behavior for ZC_PP_Shift_Adm alias Shift
use etag
{
  use create;
  use update;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}
